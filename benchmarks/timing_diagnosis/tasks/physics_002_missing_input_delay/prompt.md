# Task

Diagnose why this timing run is not signoff-ready.

Use the files under `input/`. Return only JSON matching the timing-diagnosis answer schema.

Include `unconstrained_input_count` as a top-level numeric field if the report identifies unconstrained inputs.
